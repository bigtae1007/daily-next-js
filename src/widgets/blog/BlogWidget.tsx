import React from "react";
import RefreshGoogleToken from "src/features/blog/RefreshGoogleToken";

const BlogWidget = () => {
  return (
    <div>
      <RefreshGoogleToken />
    </div>
  );
};

export default BlogWidget;
